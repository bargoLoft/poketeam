import { calcHP, calcStat, calcSpeed, calcDamageRange } from './src/utils/damageCalc.js';

// Case 1: Mega Charizard X (Adamant A252) Flare Blitz -> Cresselia (Bold H252 B4)
// Charizard X: Base Atk 130
// Cresselia: Base HP 120, Base Def 120

// We need to mock the typeUtils and other imports if we run this directly, 
// so maybe it's easier to just rely on the math.
console.log('Test file created');
